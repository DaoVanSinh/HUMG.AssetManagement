<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HUMG.AssetManagement.Views.AssetDetailPage"
             Title="Chi tiết Tài sản"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="0" Spacing="0">

            <!-- Ảnh tài sản -->
            <Border HeightRequest="250"
                    BackgroundColor="#0066CC"
                    StrokeThickness="0">
                <Grid>
                    <Image x:Name="AssetImage"
                           Aspect="AspectFill"
                           Source="placeholder_image.png"/>
                    <Border BackgroundColor="#80000000"
                            Padding="15"
                            VerticalOptions="End"
                            StrokeThickness="0">
                        <VerticalStackLayout Spacing="5">
                            <Label x:Name="AssetNameLabel"
                                   Text="Tên tài sản"
                                   FontSize="24"
                                   FontAttributes="Bold"
                                   TextColor="White"/>
                            <Label x:Name="AssetCodeLabel"
                                   Text="Mã: TS001"
                                   FontSize="16"
                                   TextColor="White"/>
                        </VerticalStackLayout>
                    </Border>
                </Grid>
            </Border>

            <!-- Thông tin chi tiết -->
            <VerticalStackLayout Padding="20" Spacing="20">

                <!-- Trạng thái và giá -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                    <Border Grid.Column="0"
                            BackgroundColor="White"
                            StrokeThickness="0"
                            Padding="15"
                            StrokeShape="RoundRectangle 12">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Trạng thái" FontSize="12" TextColor="#999"/>
                            <Border x:Name="StatusBorder"
                                    Padding="10,5"
                                    BackgroundColor="#4CAF50"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 5"
                                    HorizontalOptions="Start">
                                <Label x:Name="StatusLabel"
                                       Text="Đang sử dụng"
                                       FontSize="14"
                                       TextColor="White"
                                       FontAttributes="Bold"/>
                            </Border>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Column="1"
                            BackgroundColor="White"
                            StrokeThickness="0"
                            Padding="15"
                            StrokeShape="RoundRectangle 12">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Giá mua" FontSize="12" TextColor="#999"/>
                            <Label x:Name="PriceLabel"
                                   Text="0đ"
                                   FontSize="20"
                                   FontAttributes="Bold"
                                   TextColor="#FF9800"/>
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <!-- Thông tin cơ bản -->
                <Border BackgroundColor="White"
        StrokeThickness="0"
        Padding="15"
        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="15">
                        <Label Text="THÔNG TIN CƠ BẢN"
               FontSize="16"
               FontAttributes="Bold"
               TextColor="#0066CC"/>

                        <Grid ColumnDefinitions="120,*" 
              RowDefinitions="Auto,Auto,Auto,Auto"
              RowSpacing="12">

                            <!-- Loại tài sản -->
                            <Label Grid.Row="0" Grid.Column="0" 
                   Text="Loại tài sản:" 
                   TextColor="#666" 
                   FontSize="14"
                   VerticalOptions="Start"/>
                            <Label Grid.Row="0" Grid.Column="1" 
                   x:Name="CategoryLabel" 
                   Text="-" 
                   TextColor="#333" 
                   FontSize="14" 
                   FontAttributes="Bold"
                   VerticalOptions="Start"/>

                            <!-- Vị trí -->
                            <Label Grid.Row="1" Grid.Column="0" 
                   Text="Vị trí:" 
                   TextColor="#666" 
                   FontSize="14"
                   VerticalOptions="Start"/>
                            <Label Grid.Row="1" Grid.Column="1" 
                   x:Name="LocationLabel" 
                   Text="-" 
                   TextColor="#333" 
                   FontSize="14"
                   VerticalOptions="Start"/>

                            <!-- Phòng ban -->
                            <Label Grid.Row="2" Grid.Column="0" 
                   Text="Phòng ban:" 
                   TextColor="#666" 
                   FontSize="14"
                   VerticalOptions="Start"/>
                            <Label Grid.Row="2" Grid.Column="1" 
                   x:Name="DepartmentLabel" 
                   Text="-" 
                   TextColor="#333" 
                   FontSize="14"
                   VerticalOptions="Start"/>

                            <!-- Ngày mua -->
                            <Label Grid.Row="3" Grid.Column="0" 
                   Text="Ngày mua:" 
                   TextColor="#666" 
                   FontSize="14"
                   VerticalOptions="Start"/>
                            <Label Grid.Row="3" Grid.Column="1" 
                   x:Name="PurchaseDateLabel" 
                   Text="-" 
                   TextColor="#333" 
                   FontSize="14"
                   VerticalOptions="Start"/>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Mô tả -->
                <Border BackgroundColor="White"
                        StrokeThickness="0"
                        Padding="15"
                        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="MÔ TẢ"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#0066CC"/>
                        <Label x:Name="DescriptionLabel"
                               Text="Chưa có mô tả"
                               TextColor="#666"
                               FontSize="14"/>
                    </VerticalStackLayout>
                </Border>

                <!-- QR Code -->
                <Border BackgroundColor="White"
                        StrokeThickness="0"
                        Padding="15"
                        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="MÃ QR"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#0066CC"/>
                        <Border HeightRequest="200"
                                WidthRequest="200"
                                BackgroundColor="#F5F5F5"
                                StrokeThickness="1"
                                Stroke="#DDDDDD"
                                StrokeShape="RoundRectangle 10"
                                HorizontalOptions="Center">
                            <Image x:Name="QRCodeImage"
                                   Aspect="AspectFit"
                                   HeightRequest="180"
                                   WidthRequest="180"/>
                        </Border>
                        <Label Text="Quét mã QR để xem thông tin tài sản"
                               FontSize="12"
                               TextColor="#999"
                               HorizontalTextAlignment="Center"/>
                    </VerticalStackLayout>
                </Border>

                <!-- Lịch sử thay đổi -->
                <Border BackgroundColor="White"
                        StrokeThickness="0"
                        Padding="15"
                        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="10">
                        <HorizontalStackLayout Spacing="10">
                            <Label Text="LỊCH SỬ THAY ĐỔI"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="#0066CC"
                                   HorizontalOptions="Start"
                                   VerticalOptions="Center"/>
                            <Label x:Name="HistoryCountLabel"
                                   Text="(0)"
                                   FontSize="14"
                                   TextColor="#999"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>

                        <CollectionView x:Name="HistoryCollectionView"
                                       HeightRequest="200">
                            <CollectionView.EmptyView>
                                <Label Text="Chưa có lịch sử thay đổi"
                                       TextColor="#999"
                                       HorizontalTextAlignment="Center"
                                       Margin="0,20,0,0"/>
                            </CollectionView.EmptyView>

                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,5,0,5"
                                            Padding="10"
                                            BackgroundColor="#F8F8F8"
                                            StrokeThickness="0"
                                            StrokeShape="RoundRectangle 8">
                                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                                            <Border Grid.Column="0"
                                                    HeightRequest="40"
                                                    WidthRequest="40"
                                                    BackgroundColor="{Binding ActionColor}"
                                                    StrokeThickness="0"
                                                    StrokeShape="RoundRectangle 20">
                                                <Label Text="{Binding ActionIcon}"
                                                       FontSize="20"
                                                       HorizontalOptions="Center"
                                                       VerticalOptions="Center"/>
                                            </Border>
                                            <VerticalStackLayout Grid.Column="1" Spacing="3">
                                                <Label Text="{Binding Description}"
                                                       FontSize="14"
                                                       TextColor="#333"
                                                       FontAttributes="Bold"/>
                                                <Label Text="{Binding ActionDate, StringFormat='{0:dd/MM/yyyy HH:mm}'}"
                                                       FontSize="12"
                                                       TextColor="#999"/>
                                            </VerticalStackLayout>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- Action Buttons -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,10,0,20">
                    <Button Grid.Column="0"
                            Text="✏️ Sửa"
                            BackgroundColor="#0066CC"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="50"
                            FontSize="16"
                            Clicked="OnEditClicked"/>
                    <Button Grid.Column="1"
                            Text="🗑️ Xóa"
                            BackgroundColor="#F44336"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="50"
                            FontSize="16"
                            Clicked="OnDeleteClicked"/>
                </Grid>

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>