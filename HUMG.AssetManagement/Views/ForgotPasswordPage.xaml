<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HUMG.AssetManagement.Views.ForgotPasswordPage"
             Title="Quên mật khẩu"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Icon -->
            <Border HeightRequest="100"
                    WidthRequest="100"
                    StrokeThickness="0"
                    HorizontalOptions="Center"
                    BackgroundColor="#FFF3E0"
                    StrokeShape="RoundRectangle 50">
                <Label Text="🔐"
                       FontSize="48"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"/>
            </Border>

            <!-- Tiêu đề -->
            <Label Text="Quên mật khẩu?"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#0066CC"
                   HorizontalTextAlignment="Center"/>

            <Label Text="Nhập tên đăng nhập và email để đặt lại mật khẩu"
                   FontSize="14"
                   TextColor="#666666"
                   HorizontalTextAlignment="Center"
                   Margin="0,0,0,20"/>

            <!-- Form -->
            <Border StrokeThickness="0"
                    BackgroundColor="White"
                    Padding="20"
                    StrokeShape="RoundRectangle 15">
                <VerticalStackLayout Spacing="15">

                    <!-- Tên đăng nhập -->
                    <Label Text="Tên đăng nhập *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="UsernameEntry"
                               Placeholder="Nhập tên đăng nhập"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"/>
                    </Border>

                    <!-- Email -->
                    <Label Text="Email *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="EmailEntry"
                               Placeholder="Nhập email đã đăng ký"
                               Keyboard="Email"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"/>
                    </Border>

                    <!-- Mật khẩu mới -->
                    <Label Text="Mật khẩu mới *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="NewPasswordEntry"
                               Placeholder="Nhập mật khẩu mới"
                               IsPassword="True"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"/>
                    </Border>

                    <!-- Xác nhận mật khẩu -->
                    <Label Text="Xác nhận mật khẩu *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="ConfirmPasswordEntry"
                               Placeholder="Nhập lại mật khẩu mới"
                               IsPassword="True"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"/>
                    </Border>

                    <!-- Nút đặt lại mật khẩu -->
                    <Button Text="Đặt lại mật khẩu"
                            BackgroundColor="#0066CC"
                            TextColor="White"
                            FontSize="16"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            HeightRequest="50"
                            Clicked="OnResetPasswordClicked"
                            Margin="0,20,0,0"/>

                    <!-- Quay lại đăng nhập -->
                    <Label Text="Quay lại đăng nhập"
                           TextColor="#0066CC"
                           HorizontalTextAlignment="Center"
                           FontSize="14"
                           Margin="0,10,0,0">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnBackToLoginTapped"/>
                        </Label.GestureRecognizers>
                    </Label>

                </VerticalStackLayout>
            </Border>

            <!-- Lưu ý -->
            <Border BackgroundColor="#FFF3E0"
                    StrokeThickness="0"
                    Padding="15"
                    StrokeShape="RoundRectangle 10">
                <HorizontalStackLayout Spacing="10">
                    <Label Text="ℹ️" FontSize="20" VerticalOptions="Start"/>
                    <Label Text="Để đảm bảo bảo mật, vui lòng nhập đúng tên đăng nhập và email đã đăng ký."
                           FontSize="12"
                           TextColor="#666"
                           LineBreakMode="WordWrap"
                           VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>