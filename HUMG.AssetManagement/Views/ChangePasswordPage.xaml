<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HUMG.AssetManagement.Views.ChangePasswordPage"
             Title="Đổi mật khẩu"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Icon -->
            <Border HeightRequest="100"
                    WidthRequest="100"
                    StrokeThickness="0"
                    HorizontalOptions="Center"
                    BackgroundColor="#E3F2FD"
                    StrokeShape="RoundRectangle 50">
                <Label Text="🔑"
                       FontSize="48"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"/>
            </Border>

            <!-- Tiêu đề -->
            <Label Text="Đổi mật khẩu"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#0066CC"
                   HorizontalTextAlignment="Center"/>

            <Label Text="Vui lòng nhập mật khẩu hiện tại và mật khẩu mới"
                   FontSize="14"
                   TextColor="#666666"
                   HorizontalTextAlignment="Center"
                   Margin="0,0,0,20"/>

            <!-- Form -->
            <Border StrokeThickness="0"
                    BackgroundColor="White"
                    Padding="20"
                    StrokeShape="RoundRectangle 15">
                <VerticalStackLayout Spacing="15">

                    <!-- Thông tin user -->
                    <Border BackgroundColor="#F8F8F8"
                            StrokeThickness="0"
                            Padding="15"
                            StrokeShape="RoundRectangle 10">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Tài khoản đang đăng nhập:" 
                                   FontSize="12" 
                                   TextColor="#999"/>
                            <Label x:Name="UsernameLabel"
                                   Text="admin"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="#0066CC"/>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Mật khẩu hiện tại -->
                    <Label Text="Mật khẩu hiện tại *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="CurrentPasswordEntry"
                               Placeholder="Nhập mật khẩu hiện tại"
                               IsPassword="True"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"/>
                    </Border>

                    <!-- Mật khẩu mới -->
                    <Label Text="Mật khẩu mới *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="NewPasswordEntry"
                               Placeholder="Nhập mật khẩu mới (tối thiểu 6 ký tự)"
                               IsPassword="True"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"
                               TextChanged="OnPasswordTextChanged"/>
                    </Border>

                    <!-- Độ mạnh mật khẩu -->
                    <HorizontalStackLayout Spacing="10" x:Name="PasswordStrengthContainer" IsVisible="False">
                        <Label Text="Độ mạnh:" FontSize="12" TextColor="#666" VerticalOptions="Center"/>
                        <ProgressBar x:Name="PasswordStrengthBar" 
                                    Progress="0" 
                                    ProgressColor="#F44336"
                                    WidthRequest="150"
                                    HeightRequest="8"
                                    VerticalOptions="Center"/>
                        <Label x:Name="PasswordStrengthLabel" 
                               Text="Yếu" 
                               FontSize="12" 
                               TextColor="#F44336"
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>

                    <!-- Xác nhận mật khẩu mới -->
                    <Label Text="Xác nhận mật khẩu mới *" FontSize="14" FontAttributes="Bold" TextColor="#333"/>
                    <Border StrokeThickness="1" 
                            Stroke="#DDDDDD" 
                            BackgroundColor="White" 
                            StrokeShape="RoundRectangle 10">
                        <Entry x:Name="ConfirmPasswordEntry"
                               Placeholder="Nhập lại mật khẩu mới"
                               IsPassword="True"
                               PlaceholderColor="#999999"
                               TextColor="#333333"
                               FontSize="16"
                               Margin="15,10"/>
                    </Border>

                    <!-- Nút đổi mật khẩu -->
                    <Button Text="Đổi mật khẩu"
                            BackgroundColor="#0066CC"
                            TextColor="White"
                            FontSize="16"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            HeightRequest="50"
                            Clicked="OnChangePasswordClicked"
                            Margin="0,20,0,0"/>

                    <!-- Nút hủy -->
                    <Button Text="Hủy"
                            BackgroundColor="#F5F5F5"
                            TextColor="#666666"
                            FontSize="16"
                            CornerRadius="10"
                            HeightRequest="50"
                            Clicked="OnCancelClicked"/>

                </VerticalStackLayout>
            </Border>

            <!-- Yêu cầu mật khẩu -->
            <Border BackgroundColor="#E8F5E9"
                    StrokeThickness="0"
                    Padding="15"
                    StrokeShape="RoundRectangle 10">
                <VerticalStackLayout Spacing="8">
                    <Label Text="Yêu cầu mật khẩu:"
                           FontSize="14"
                           FontAttributes="Bold"
                           TextColor="#333"/>
                    <Label Text="• Tối thiểu 6 ký tự"
                           FontSize="12"
                           TextColor="#666"/>
                    <Label Text="• Nên có chữ hoa, chữ thường và số"
                           FontSize="12"
                           TextColor="#666"/>
                    <Label Text="• Không nên trùng với mật khẩu cũ"
                           FontSize="12"
                           TextColor="#666"/>
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>